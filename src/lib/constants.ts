/**
 * Shared constants for API routes
 */

// Maximum length for text input (OpenAI TTS limit is 4096)
export const MAX_INPUT_LENGTH = 4096;

// Maximum length for instruction/prompt text
export const MAX_PROMPT_LENGTH = 1000;
